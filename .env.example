# PgBoss Tasks 环境变量配置示例
# 复制此文件为 .env 并修改为实际值

# ============================================
# 方式 1：使用完整的 DATABASE_URL（推荐用于外部数据库）
# ============================================
# 如果使用外部数据库，取消下面的注释并配置
# DATABASE_URL=postgresql://user:password@host:5432/database

# ============================================
# 方式 2：使用 POSTGRES_* 变量（推荐用于 docker-compose）
# ============================================
# 如果使用 docker-compose 中的 postgres 服务，配置以下变量
# DATABASE_URL 会自动从这些变量生成，无需手动拼接

# 数据库主机（docker-compose 中使用 postgres 服务名）
POSTGRES_HOST=postgres

# 数据库端口
POSTGRES_PORT=5432

# 数据库用户名
POSTGRES_USER=user

# 数据库密码（特殊字符会自动进行 URL 编码，无需手动处理）
POSTGRES_PASSWORD=password

# 数据库名称
POSTGRES_DB=database

# ============================================
# API 服务器配置
# ============================================

# API 服务器端口（默认: 3001）
PORT=3001

# ============================================
# 使用说明
# ============================================
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 文件，修改为实际配置值
# 3. .env 文件已添加到 .gitignore，不会被提交到版本控制
#
# 配置方式选择：
# - 使用外部数据库：设置 DATABASE_URL，注释掉 POSTGRES_* 变量
# - 使用 docker-compose：设置 POSTGRES_* 变量，DATABASE_URL 会自动生成
#
# 注意：如果同时设置了 DATABASE_URL 和 POSTGRES_* 变量，
#       DATABASE_URL 会优先使用